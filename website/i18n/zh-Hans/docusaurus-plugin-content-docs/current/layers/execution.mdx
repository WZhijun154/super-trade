---
title: 执行（execution）
---

import BrowserOnly from '@docusaurus/BrowserOnly';

# 执行 — `super_trade.execution`

实盘交易层。它让策略通过**券商**（broker）运行，并带有风控；它也是回测与实盘之间的桥梁：
**同一个** `Strategy` 既能驱动模拟券商（dry-run / 回测），也能驱动 QMT 券商（在
**模拟账户**上实盘）。

:::danger[敏感层]
该层会下单。默认 **`dry_run=True`**（只记录订单、不真正发送），且仅面向
**QMT 模拟账户**。请保持 `RiskManager` 限额开启，只有在确认 dry-run 输出后才设为
`dry_run=False`。
:::

## 与回测的桥梁

回测与实盘共用同一条代码路径——只换 broker：

<BrowserOnly fallback={<div>正在加载架构图…</div>}>
  {() => {
    const BridgeDiagram = require('@site/src/components/BridgeDiagram').default;
    return <BridgeDiagram />;
  }}
</BrowserOnly>

`Broker` 暴露 `cash()`、`positions()`、`place_order()`。`SimBroker` 针对内存中的
`Account`（现金 + 以真实手/股计的持仓）撮合，并复用回测的 `CostModel`，因此成本一致。
`QmtBroker` 通过 `xtquant.xttrader` 下单。

## 扫描循环

`ExecutionEngine.scan_once()` 是对股票池的一次扫描：

1. 逐 symbol 读取近期 bar → 计算策略目标权重与价格；
2. 按市值标记账户；应用**单日亏损熔断**；
3. **先处理止损平仓**（当根扫描刚止损的标的不再被重新买入）；
4. **信号驱动的开/平仓**，由 `RiskManager` 决定仓位大小；
5. 通过 broker 下单（dry-run 时仅记录）。

```python
from super_trade.execution import SimBroker, RiskManager, ExecutionEngine
from super_trade.backtest import SmaCross

engine = ExecutionEngine(
    broker=SimBroker(cash=1_000_000),
    store=store, strategy=SmaCross(10, 30), risk=RiskManager(),
    universe=["600519", "000001"], dry_run=True,
)
engine.scan_once()              # 一次扫描
engine.run(interval_seconds=10) # 每 10 秒扫描一次（阻塞；Ctrl-C 停止）
```

## 风险管理

`RiskManager` 是**策略信号与真实订单之间的安全层**。策略只决定*是否*做多；`RiskManager`
决定*能买多少*、以及*是否拦截或强制*某笔交易——把风控规则集中到一处。它由事件驱动回测
与实盘执行使用（向量化引擎不用它）。

### 限额（`RiskLimits`）

| 限额 | 默认值 | 含义 |
|---|---|---|
| `max_position_weight` | 0.20 | 单标的最多占权益的 20% |
| `max_gross_exposure` | 0.95 | 总仓位最多 95%（保留现金缓冲） |
| `max_daily_loss` | 0.05 | 当日回撤超过 5% 即停止开新仓 |
| `stop_loss` | 0.08 | 某标的相对入场价跌 8% 即平仓 |
| `max_orders_per_day` | 200 | 每个交易日的订单上限 |

### 它做什么

- **仓位定量**——`max_buy_shares(symbol, price, equity, broker)` 返回允许的最大**按手
  取整**买入量：取三个预算的**最小值**，再向下取整到整手
  （`shares = (budget // (price × 100)) × 100`）：
  - *单标的*：`max_position_weight × equity` − 已持有部分，
  - *总敞口*：`max_gross_exposure × equity` − 已投入总额，
  - 你的可用**现金**。
- **止损**——当 `price ≤ avg_price × (1 − stop_loss)` 时 `stop_triggered(pos, price)`
  为真；引擎随即卖出该标的。
- **单日亏损熔断**——`start_day(equity)` 记录开盘权益；`update_daily_loss(equity)` 在
  权益跌破 `max_daily_loss` 时熔断。
- **订单数上限**——超过 `max_orders_per_day` 后 `can_trade()` 变为假。
- **急停开关**——`kill()` 立即全部熔断。

### 只拦买入，平仓永不拦

熔断时（单日亏损、急停、或订单上限），**新买入被阻止，但卖出与止损始终放行**——你随时
都能降低风险。（`can_trade()` 在开仓前检查；平仓绕过它。）

### 按会话保存状态

`RiskManager` 携带日内状态（当日起始权益、订单计数、熔断标志），因此**每次回测/会话都新建
一个**，并在开盘时调用 `start_day()`——事件驱动回测与 `ExecutionEngine` 会替你处理。

```python
from super_trade.execution import RiskManager, RiskLimits

risk = RiskManager(RiskLimits(max_position_weight=0.15, stop_loss=0.06))
```

## 每日报告

`daily_report(broker, fills, prices)` 生成 Markdown 摘要（权益、成交、成本、持仓、浮动
盈亏）——可在收盘时由定时任务生成。

## 在 QMT 模拟账户上实盘

1. 在装有 **MiniQMT** 的机器上配置 `QmtBroker(account_id=..., mini_qmt_path=...)`，
   并验证 `xttrader` 调用（已搭好骨架——必须在该机器上核对）。
2. 先以 `dry_run=True` 运行并观察记录的订单。
3. 再切换为 `dry_run=False` 配合 `QmtBroker` → 订单进入你的**模拟**账户。

:::caution[止损无法用向量化引擎回测]
止损是路径依赖的，因此[向量化回测](./backtest)无法验证它。这里的 `SimBroker` 正是可以
验证它的事件驱动路径的起点——在实盘信任止损之前先回测它。
:::
